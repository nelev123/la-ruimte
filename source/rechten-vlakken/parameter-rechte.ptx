<?xml version="1.0" encoding="UTF-8"?>

<section xml:id="sec-parameter-rechte">
  <title>Parametervoorstelling en cartesische vergelijkingen van een rechte</title>

  <subsection>
    <title>Van vectoriële vergelijking naar parametervoorstelling</title>
    <p>
      We kiezen nu een affien assenstelsel. De richtingsvector <m>\vv{R}</m> is ondubbelzinnig bepaald door zijn coördinaten t.o.v. dit assenstelsel:
        <me>co(\vv{R})=(a,b,c) \qquad \neg(a=b=c=0)</me>
       We noemen <m>(a,b,c)</m> een stel <term>richtingsgetallen</term> van de rechte met vectoriële vergelijking
      <m>
        \vv{P}=\vv{P_1}+k\vv{R}
      </m>. Een rechte heeft oneindig veel richtingsgetallen aangezien elke vector die een veelvoud is van <m>\vv{R}</m> eveneens een richtingsvector is van de rechte.
    </p>

    <aside>
      <title>Notatie</title>
      <p>
          <m>\neg</m> is het symbool voor niet
      </p>
      <p>
          <m>\wedge</m> is het symbool voor en
      </p>
    </aside>

    <assemblage>
      <title>Evenwijdige rechten</title>
      <p>
        De rechte <m>r</m> met richtingsvector <m>\vv{R}(a_1,b_1,c_1)</m> en de rechte <m>s</m> met richtingsvector <m>\vv{S}(a_2,b_2,c_2)</m> zijn evenwijdig als en slechts als hun stellen richtingsgetallen evenredig zijn:
        <me>r \parallel s \Leftrightarrow \vv{S}=k\vv{R} \Leftrightarrow a_2=k a_1 \wedge b_2 = k b_1  \wedge c_2 = k c_1 \quad (k \in \mathbb{R}_0)</me>
      </p>
    </assemblage>

    <p>
      We beschouwen de rechte <m>r</m> die gaat door het punt <m>P_1(x_1,y_1,z_1)</m> en evenwijdig is met de vector <m>\vv{R}(a,b,c)</m>. De coördinaten van een willekeurig punt <m>P(x,y,z)</m> van deze rechte zijn dan
      <md>
        <mrow>\amp co(\vv{P})=co(\vv{P_1})+k \cdot co(\vv{R}) </mrow>
        <mrow>\Leftrightarrow \amp (x,y,z)=(x_1,y_1,z_1)+k(a,b,c)</mrow>
        <mrow>\Leftrightarrow \amp \boxed{\begin{cases} x = x_1+k \cdot a \\ y=y_1+k \cdot b \\ z=z_1+k \cdot c  \end{cases} }\qquad (k \in \mathbb{R})</mrow>
      </md>
      Dit is de parametervoorstelling van de rechte <m>r</m>.
    </p>

    <p>
      Voor een rechte door twee gegeven punten <m>P_1(x_1,y_1,z_1)</m> en <m>P_1(x_2,y_2,z_2)</m> is <m>\vv{R}=\vv{P_2}-\vv{P_1}</m> en wordt de parametervoorstelling<fn>Dit volgt ook onmiddelijk uit de vectoriële vergelijking van een rechte door twee punten.</fn>
      <me>
        \begin{cases} x = x_1+k (x_2-x_1) \\ y=y_1+k (y_2-y_1)  \\ z=z_1+k (z_2-z_1)  \end{cases}  \Leftrightarrow \boxed{\begin{cases} x = (1-k)x_1 +k  x_2\\ y= (1-k) y_1+k y_2 \\ z= (1-k) z_1+k  z_2 \end{cases}} \qquad (k \in \mathbb{R})
      </me>    
    </p>
         <example>
      <p>
        We beschouwen de rechte door het punt <m>P_1(-2,5,-1)</m> en met richtingsgetallen <m>(6,-6,2)</m>. Een parametervoorstelling van deze rechte is
        <me>
          \begin{cases}
          x=-2+6k \\
          y=5-6k \\
          z= -1 + 2k
          \end{cases} \qquad (k \in \mathbb{R})
        </me>
        Een ander stel richtingsgetallen is <m>(-3,3,-1)</m> en een tweede parametervoorstelling is bijgevolg
        <me>
          \begin{cases}
          x=-2-3k \\
          y=5+3k \\
          z= -1 -k
          \end{cases} \qquad (k \in \mathbb{R})
        </me>
        Er zijn ook een oneindig aantal mogelijkheden voor <m>P_1</m>; dit is immers gewoon een willekeurig punt van de rechte. Stellen we in de tweede parametervoorstelling <m>k=-2</m> dan krijgen we
        <me>
          \begin{cases}
          x=-2-3(-2)=4 \\
          y=5+3(-2)=-1 \\
          z= -1 -(-2)=1
          \end{cases} 
        </me> 
        en een derde mogelijke parametervoorstelling van deze rechte is
        <me>
          \begin{cases}
          x=4-3k \\
          y=-1+3k \\
          z= 1 -k
          \end{cases} \qquad (k \in \mathbb{R})
        </me>       
      </p>
      <p>
        Met behulp van de parametervoorstelling kan je snel controleren of een gegeven punt op de rechte ligt. Het punt <m>(1,2,3)</m>, bijvoorbeeld, ligt niet op de rechte omdat
        <me>
          \begin{cases}
          1=4-3k \\
          2=-1+3k \\
          3= 1 -k
          \end{cases}  \Leftrightarrow
          \begin{cases}
          k=1 \\
          k=1 \\
          k= -2
          \end{cases}
        </me>  
      </p>
      <p>
        We bepalen tenslotte het snijpunt van deze rechte met het <m>xy</m>-vlak. De <m>z</m>-coördinaat van dit snijpunt is nul en hieruit volgt de waarde van de parameter
        <me>
          0=1-k \Leftrightarrow k=1
        </me>
        Invullen in de parametervoorstelling geeft het punt <m>(1,2,0)</m>
        <me>
          \begin{cases}
          x=4-3=1 \\
          y=-1+3=2 \\
          z= 0
          \end{cases} \qquad (k \in \mathbb{R})
        </me> 
      </p>
    </example> 

    <activity xml:id="activ-kubus-3">
      <statement>
        <p>
          Beschouw onderstaande kubus. Kies een geschikt assenstelsel en stel parametervoorstellingen op van de rechten <m>DG</m>, <m>DH</m>, <m>GH</m>, <m>EC</m> en <m>DB</m>.
        </p>
        <sidebyside width="70%">
          <image source="par-rechte-kubus.png"/>
        </sidebyside>
      </statement>
    </activity>
    
  </subsection>

  <subsection xml:id="subsec-cartesisch-rechte">
    <title>Van parametervoorstelling naar cartesische vergelijkingen</title>
    <example>
    <p>
        Een stelsel <fn>Je hebt twee lineaire vergelijkingen nodig omdat een rechte de doorsnede is van twee vlakken.</fn> cartesische vergelijkingen van een rechte vinden we door een parametervoorstelling te beschouwen als een stelsel in <m>k</m> en de voorwaarde op te stellen waarvoor dit stelsel een unieke oplossing heeft <mdash/> we noemen dit het elimineren van de parameter. We lossen dus elke parametervergelijking eerst op naar <m>k</m> en drukken vervolgens uit dat de oplossing uniek moet zijn. Voor de rechte met parametervoorstelling
        <me>
          \begin{cases}
          x=-2+6k \\
          y=5-6k \\
          z= -1 + 2k
          \end{cases} \qquad (k \in \mathbb{R})
        </me>
        geeft dit
        <me>
          \begin{cases}
            k=\dfrac{x+2}{6}\\
            k=\dfrac{y-5}{-6}\\
            k=\dfrac{z+1}{2}
          \end{cases} \Leftrightarrow \frac{x+2}{6}=\frac{y-5}{-6}=\frac{z+1}{2}
        </me>
        Merk op dat dit een stelsel is van twee lineaire vergelijkingen in <m>x</m>, <m>y</m> en <m>z</m>. We kunnen dit stelsel bijvoorbeeld ook als volgt noteren:
        <me>
          \begin{cases}
           x+y=3 \\
           y+3z=2
          \end{cases}
        </me>
        en we kunnen stellen dat deze rechte de snijlijn is van de vlakken <m>x+y=3</m> en <m>y+3z=2</m>. 
        <!--geogebra stukje toevoegen-->
    </p>
    </example>
    <p>
      We beschouwen de rechte door het punt <m>P_1(x_1,y_1,z_1)</m> met richtingsvector <m>\vv{R}(a,b,c)</m>. De parametervoorstelling is:
      <me>
         \begin{cases} x = x_1+k \cdot a \\ y=y_1+k \cdot b \\ z=z_1+k \cdot c \end{cases} \quad (k \in \mathbb{R})
      </me> 
      Om de cartesische vergelijkingen te bepalen elimineren we de parameter <m>k</m>.
      <me>
        \begin{cases} k = \dfrac{x-x_1}{a} \\ k = \dfrac{y-y_1}{b} \\ k = \dfrac{z-z_1}{c} \end{cases} \Leftrightarrow \boxed{\frac{x-x_1}{a}=\frac{y-y_1}{b}=\frac{z-z_1}{c}}
      </me>
       We krijgen nu de zogenaamde <term>standaardvorm</term> van het stelsel cartesische vergelijkingen.
    </p>
    <remark>
      <p>
        Let op! Als één van de richtingsgetallen nul is, betekent dat niet dat het stelsel cartesische vergelijkingen niet bestaat. Je grijpt dan terug naar de parametervoorstelling om dit stelsel te bepalen.
      </p>
    </remark>
    <example>
      <p>
        We beschouwen de rechte door het punt <m>P_1(5,1,6)</m> en met richtingsgetallen <m>(2,0,0)</m>. Een parametervoorstelling van deze rechte is
        <me>
          \begin{cases}
          x=5+2k \\
          y=1 \\
          z= 6
          \end{cases} \qquad (k \in \mathbb{R})
        </me>
        Het stelsel cartesische vergelijkingen is bijgevolg
        <me>
          \begin{cases}
           y=1\\
           z=6
          \end{cases}
        </me>
      </p>
    </example>

    <remark>
      <p>
        De parametervoorstelling <m> \begin{cases} x = x_1+k \cdot a \\ y=y_1+k \cdot b \\ z=z_1+k \cdot c \end{cases} \quad (k \in \mathbb{R})
        </m> kan beschouwd worden als een <m>3 \times 1</m> stelsel in de onbekende <m>k</m> met als uitgebreide matrix <m>\begin{bmatrix} a \amp  x-x_1 \\ b \amp  y-y_1 \\ c \amp z-z_1 \end{bmatrix}</m>. We bepalen de rij-canonieke vorm 
        <me>
          \begin{bmatrix} a \amp  x-x_1 \\ b \amp  y-y_1 \\ c \amp z-z_1 \end{bmatrix} \sim \begin{bmatrix} 1 \amp  \frac{x-x_1}{a} \\ 0 \amp  y-y_1-\frac{b}{a}(x-x_1) \\ 0 \amp z-z_1-\frac{c}{a}(x-x_1) \end{bmatrix}
        </me>
        Het stelsel heeft precies één oplossing als <m>r(A)=r(A_b)=1</m> en dus moet er gelden dat
        <me>
          \begin{cases}
           y-y_1-\frac{b}{a}(x-x_1) = 0 \\ z-z_1-\frac{c}{a}(x-x_1) = 0 
          \end{cases}   \Leftrightarrow \begin{cases} \dfrac{x-x_1}{a}=\dfrac{y-y_1}{b}\\   \dfrac{x-x_1}{a}=\dfrac{z-z_1}{c} \end{cases}
        </me>
        en we krijgen opnieuw de standaardvorm van het stelsel cartesische vergelijkingen. We noemen het stelsel cartesische vergelijkingen de <term>coëxistentievoorwaarde</term> van het stelsel parametervergelijkingen, d.w.z. dat het stelsel parametervergelijkingen slechts een oplossing heeft als aan deze voorwaarde voldaan is. 
      </p>
    </remark>

    <example>
      <p>
        Gegeven de rechte met parametervoorstelling
         <me>
           \begin{cases}
            x = 1 + 2k  \\ y = 2 + 3k \\ z = k
          \end{cases}
         </me>
         We beschouwen dit als een <m>3 \times 1</m> stelsel in de onbekende <m>k</m> en bepalen de rij-canonieke vorm van de uitgebreide matrix
        <md>
          <mrow> \begin{bmatrix} 2 \amp x-1 \\ 3 \amp y-2 \\ 1 \amp z \end{bmatrix} \overset{R_1 \leftrightarrow R_3}{\sim} \begin{bmatrix} 1 \amp z \\ 3 \amp y-2 \\ 2 \amp x-1 \end{bmatrix} \overset{R_2 \to R_2-3R_1}{\underset{R_3 \to R_3-2R_1}{\sim}}   \begin{bmatrix} 1 \amp z \\ 0 \amp y-2-3z \\ 0 \amp x-1-2z \end{bmatrix}</mrow>
       </md>
        Het stelsel heeft precies één oplossing als <m>r(A)=r(A_b)=1</m> en dus moet er gelden dat
       <me>
        \begin{cases}
         y-2-3z=0 \\ x-1-2z=0
        \end{cases}   \Leftrightarrow \begin{cases} y-3z=2 \\ x-2z=1  \end{cases}
       </me>
      </p>
    </example>

    <activity xml:id="activ-kubus-4">
      <statement>
        <p>
          Beschouw opnieuw onderstaande kubus. Bepaal de cartesische vergelijkingen van de rechten <m>DG</m>, <m>DH</m>, <m>GH</m>, <m>EC</m> en <m>DB</m>.
        </p>
        <sidebyside width="70%">
          <image source="par-rechte-kubus.png"/>
        </sidebyside>
      </statement>
    </activity>

  </subsection>

  <subsection>
    <title>Van cartesische vergelijkingen naar parametervoorstelling</title>
    <example>
      <p>
        Gegeven de rechte met stelsel cartesische vergelijkingen
        <me>
          \begin{cases}
           x+y=3 \\
           y+3z=2
          \end{cases}
        </me>
        Een parametervoorstelling kan dan bepaald worden door één van de variabelen gelijk te stellen aan een lineaire functie in <m>k</m>. 
        Kiezen we bijvoorbeeld <m>z=k</m>, dan wordt de parametervoorstelling
        <me>
          \begin{cases}
          x=1+3k \\
          y=2-3k \\
          z=k
          \end{cases} \qquad (k \in \mathbb{R})
        </me>
        Stellen we <m>y=-4+5k</m>, dan wordt de parametervoorstelling
        <me>
          \begin{cases}
          x=7-5k \\
          y=-4+5k \\
          z=2-\dfrac{5}{3}k
          \end{cases} \qquad (k \in \mathbb{R})
        </me>
      </p>
    </example>

    <example>
      <p>
        Beschouw de rechte beschreven door de cartesische vergelijkingen
        <me>
          \begin{cases} x-y+z+1=0 \\ 2x-3y+5z+4=0 \end{cases}
        </me>
        Om hieruit een parametervoorstelling af te leiden los je dit stelsel gewoon op. De uitgebreide matrix <m>A_b</m> is
        <me>
          \begin{bmatrix} 1 \amp -1 \amp 1 \amp -1 \\ 2 \amp -3 \amp 5 \amp -4 \end{bmatrix}
        </me>
        We bepalen de rij-canonieke vorm:
        <me>
          A_b \sim \begin{bmatrix} 1 \amp 0 \amp -2 \amp 1 \\ 0 \amp 1 \amp -3 \amp 2 \end{bmatrix}
        </me>
        Het stelsel dat hiermee correspondeert is
        <me>
          \begin{cases} x-2z=1 \\ y-3z=2 \end{cases}
        </me>
        Hieruit kan nu snel de standaardvorm<fn><m>\frac{x-x_1}{a}=\frac{y-y_1}{b}=\frac{z-z_1}{c} </m></fn> van de cartesische vergelijkingen afgeleid worden:
        <me>
          \begin{cases} x-1=2z \\ y-2=3z \end{cases} \Leftrightarrow \frac{x-1}{2}=\frac{y-2}{3}=z
        </me>
        en ook een parametervoorstelling
        <me>
          \begin{cases} x=1+2k \\ y= 2+3k \\z = k \end{cases} \qquad (k \in \mathbb{R})
        </me>
      </p> 
    </example>

    <assemblage>
      <title>Parametervoorstelling van een rechte</title>
      <p>
        De rechte door het punt <m>P_1(x_1,y_1)</m> en evenwijdig met de vector <m>\vv{R}(a,b,c)</m> heeft als parametervoorstelling
        <me>
          \begin{cases} x = x_1+k \cdot a \\ y=y_1+k \cdot b \\ z=z_1+k \cdot c  \end{cases} \qquad (k \in \mathbb{R})
        </me>
        <m>(a,b,c)</m> noemen we een stel richtingsgetallen van de rechte.
      </p>
      <p>
        De rechte door de punten <m>P_1(x_1,y_1,z_1)</m> en <m>P_2(x_2,y_2,z_2)</m>  heeft als parametervoorstelling
        <me>
          \begin{cases} x = x_1+k (x_2-x_1) = (1-k)x_1 +k  x_2\\ y=y_1+k (y_2-y_1) = (1-k) y_1+k y_2 \\ z=z_1+k (z_2-z_1) = (1-k) z_1+k  z_2 \end{cases} \qquad (k \in \mathbb{R})
        </me>
      </p>  
    </assemblage>

    <assemblage>
      <title>Cartesische vergelijkingen van een rechte</title>
      <p>
        De rechte <m>r</m> door het punt <m>P_1(x_1,y_1,z_1)</m> en evenwijdig met de vector <m>\vv{R}(a,b,c)</m> heeft als stelsel cartesische vergelijkingen
        <me>
        \frac{x-x_1}{a}=\frac{y-y_1}{b}=\frac{z-z_1}{c} \quad (a,b,c \neq 0)
        </me>
      </p>
    </assemblage>

    <assemblage>
      <title>Stelsels en vergelijkingen van rechten</title>
      <sidebyside width="80%">
        <image xml:id="fig-stelsel-parameter-1">
          <latex-image>
            <![CDATA[
            \begin{tikzpicture}
            \node[text width=4cm] at (-2,0) {parametervoorstelling ($3 \times 1$ stelsel)};
            \draw[>=latex,->,shorten >= 5pt,thick] (0,0) to [out=40,in=140] node[pos=0.5,above,text width=4cm]{parameters elimineren of coëxistentievoorwaarde opstellen} (4,0);
            \draw[>=latex,->,shorten >= 5pt,thick] (4,0) to [out=-140,in=-40] node[pos=0.5,below,text width=4cm]{stelsel oplossen en parameter invoeren} (0,0);
            \node[text width=5cm] at (6.5,0) {cartesische vergelijkingen ($2 \times 3$ stelsel)};
            \end{tikzpicture}
            ]]>
          </latex-image>
        </image>
      </sidebyside>
      <p>
        Een parametervoorstelling van een rechte in de ruimte bepaal je door een <m>2 \times 3</m> stelsel cartesische vergelijkingen op te lossen. Omgekeerd is het <m>2 \times 3</m> stelsel cartesische vergelijkingen de coëxistentievoorwaarde voor het stelsel parametervergelijkingen.
      </p>
    </assemblage>
  </subsection>



</section>
